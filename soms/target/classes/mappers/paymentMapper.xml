<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="payment">

	<select id="selectOnePayInfo" parameterType="String" resultType="MemberDTO">
		SELECT
				*
		FROM
				MEMBER
		WHERE
				MEMBER_ID = #{memberId}
	</select>
	
	<insert id="insertPhonePayment" parameterType="PaymentDTO">
		INSERT INTO PAYMENT(
			MEMBER_ID,
			ORDER_NM,
			ORDER_HP,
			PAYMENT_AMOUNT,
			PAY_METHOD,
			PAY_ORDER_HP,
			MEMBERSHIP,
			DAYS_LEFT,
			PAY_ORDER_TIME			
		) VALUES (
			#{memberId},
			#{orderNm},
			#{orderHp},
			#{paymentAmount},
			#{payMethod},
			#{payOrderHp},
			#{membership},
			30,
			NOW()
		)
	</insert>
	
	<insert id="insertCardPayment" parameterType="PaymentDTO">
		INSERT INTO PAYMENT(
			MEMBER_ID,
			ORDER_NM,
			ORDER_HP,
			PAYMENT_AMOUNT,
			PAY_METHOD,
			CARD_COMPANY_NM,
			CARD_PAY_MONTH,
			MEMBERSHIP,
			DAYS_LEFT,
			PAY_ORDER_TIME			
		) VALUES (
			#{memberId},
			#{orderNm},
			#{orderHp},
			#{paymentAmount},
			#{payMethod},
			#{cardCompanyNm},
			#{cardPayMonth},
			#{membership},
			30,
			NOW()
		)	
	</insert>
	
	<update id="updateDaysLeft">
		UPDATE
				PAYMENT
		SET
				DAYS_LEFT = DAYS_LEFT - 1
	</update>
	
	<select id="selectListDaysList" resultType="PaymentDTO">
			SELECT
					*
			FROM
					PAYMENT
			WHERE
					DAYS_LEFT = 0
	</select>
	
	<delete id="deleteMembership" parameterType="PaymentDTO">
		DELETE FROM
				PAYMENT
		WHERE
				MEMBER_ID = #{memberId}
	</delete>
	
</mapper>