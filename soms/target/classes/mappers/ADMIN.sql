USE SOMS;

CREATE TABLE `ADMIN`(
    ADMIN_ID 	VARCHAR(20) PRIMARY KEY,
	PASSWD 		VARCHAR(100),				# NOT NULL
	JOIN_DT 	TIMESTAMP 	 				# NOT NULL
);

INSERT INTO `ADMIN` VALUES ('somsAdminId' , '1x1bkatedb36ugy8' , NOW());

SELECT * FROM SOMS.ADMIN;